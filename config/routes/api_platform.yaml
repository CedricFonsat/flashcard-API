api_platform:
    resource: .
    type: api_platform
    prefix: /api
 #   enable_docs: false
  #  show_webby: false
   # mapping:
    #    paths: [ '%kernel.project_dir%/src/Entity' ]
   # patch_formats:
    #    json: [ 'application/merge-patch+json' ]
   # swagger:
    #    versions: [ 3 ]
